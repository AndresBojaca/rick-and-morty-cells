/* eslint-disable no-unused-vars */
import { css } from 'lit-element';

export default css`

  .character-grid {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    justify-content: center;
  }
  .pagination-container{
    margin: 2rem 0;
  }
  .search-container{
      max-width: 80%;
      margin: 3rem auto;
  }

`;